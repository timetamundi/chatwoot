diff --git a/app/models/channel/telegram.rb b/app/models/channel/telegram.rbindex b00897614..1d161c1ac 100644--- a/app/models/channel/telegram.rb+++ b/app/models/channel/telegram.rb@@ -130,8 +130,12 @@ class Channel::Telegram < ApplicationRecord     # escape html tags in text. We are subbing \n to <br> since commonmark will strip exta '\n'     text = CGI.escapeHTML(text.gsub("\n", '<br>')) -    # convert markdown to html-    html = CommonMarker.render_html(text).strip+    # convert markdown to html (compativel com commonmarker novo e legado)+    html = if defined?(Commonmarker)+             Commonmarker.to_html(text)+           else+             CommonMarker.render_html(text)+           end.strip      # remove all html tags except b, strong, i, em, u, ins, s, strike, del, a, code, pre, blockquote     stripped_html = Rails::HTML5::SafeListSanitizer.new.sanitize(html, tags: %w[b strong i em u ins s strike del a code pre blockquote],diff --git a/app/models/user.rb b/app/models/user.rbindex d1907362a..2fadc0346 100644--- a/app/models/user.rb+++ b/app/models/user.rb@@ -75,7 +75,7 @@ class User < ApplicationRecord   accepts_nested_attributes_for :account_users    has_many :assigned_conversations, foreign_key: 'assignee_id', class_name: 'Conversation', dependent: :nullify, inverse_of: :assignee-  alias_attribute :conversations, :assigned_conversations+  alias conversations assigned_conversations   has_many :csat_survey_responses, foreign_key: 'assigned_agent_id', dependent: :nullify, inverse_of: :assigned_agent   has_many :conversation_participants, dependent: :destroy_async   has_many :participating_conversations, through: :conversation_participants, source: :conversation